# Frontend data mode: keep local until the backend is deployed.
VITE_DATA_MODE=local

# Production backend base URL, for example:
# VITE_API_BASE_URL=https://api.your-domain.com/api
VITE_API_BASE_URL=/api
